[project]
name = "randomcode"
version = "0.1.0"
description = "Launch or attach to project-specific tmux sessions in ~/Sync/randomcode"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
randomcode = "main:main"
